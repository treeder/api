{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "description": "Like fetch, but better. Parses JSON, stringifies and handles errors.",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "models": "github:treeder/models"
  }
}
